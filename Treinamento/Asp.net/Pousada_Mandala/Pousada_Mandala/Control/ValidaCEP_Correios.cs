using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;
using Pousada_Mandala.Model;
using System.Data;

namespace Pousada_Mandala.Control
{
    public class ValidaCEP_Correios
    {
/*
        protected ValidaCEP_Correios cepHosp;
        protected CadastHospede cadHosp;

        public ValidaCEP_Correios()
        {
            this.cepHosp = new ValidaCEP_Correios();
        }

        public  DataSet ValidaCEP_Correio(string cep)
        {
            string xml = "http://cep.republicavirtual.com.br/web cep.php?cep=@cep&formato=xml".Replace("@cep", cadHosp.cep.ToString());

            DataSet ds = new DataSet();

            ds.ReadXml(xml);

            return ds;


        }

        public int valcep()
        {
            return 1;
        }*/
    }
}